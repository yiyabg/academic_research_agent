# 本地论文库重构实施完成报告

日期：2026-08-27
基线提交：`3e1580bc6a6586828329b63f05125aa2d34c2d66`

## 结论

已按 `REFACTORING_PROGRESS.md` 核查并落地本轮修复。没有提交/推送代码，也没有改动 0052/0053 的迁移语义。经用户明确授权后，已仅使用 `/home/cumt/lly/test_library` 的单篇测试 PDF 执行真实 provider staged 分析；没有向 provider 发送当前共享论文库的内容。已在当前主机的既有 Compose 本地部署中重建后端和前端镜像、执行迁移并滚动更新 API/worker/frontend；这不是外部生产环境发布。

以下项目已实现并通过定向验证：查询解析、元数据筛选修正、analysis 的真实 BM25、discovery 两阶段软配额、token 化 evidence 上下文、Pydantic 校验的统一 evidence payload、query-only 导入修复、三种 mode 的提示与部分报告结构差异、background recovery 路由与 fake 生命周期、前端高级筛选/解释展示、旧 Mindmap 服务删除、Compose/环境变量/验证脚本/部署文档。

## 初始根因与最终边界

- discovery 保持跨论文路径：PostgreSQL 元数据预过滤 → Dense + BM25 → RRF → BGE → 论文级 MMR；它的目标是找相关论文。
- selected-paper analysis 保持逐篇路径：每个 `paper_id` 单独使用 `question` 检索 child chunk，再构建 parent context；不使用跨论文 MMR，因此已选论文不会被全局排名挤掉。
- 原有 analysis retriever 仅对 `lexical_rows[:bm25_limit]` 做全文字符串包含判断，且统一给 1.0 分；现已改为全 scope `BM25Okapi` 评分后取 top-k，再与 dense 结果 RRF 融合。discovery 与 analysis 共同使用 `local_paper_retrieval.py` 的 tokenizer、scope cache、BM25 排名和 RRF 实现，不再维护语义不同的两套 lexical 逻辑。

## 清单逐项核查

| 清单项 | 结论 | 当前证据 |
| --- | --- | --- |
| 4.1 查询解析 | 已修复 | 53 项 parser/analysis 回归及运行容器示例均验证精确年份清理与中英文约束。 |
| 4.2 元数据过滤 | 已修复 | DOI、ANY keywords、exact year、真实 total 与 metadata-only 分支均有单元覆盖。 |
| 4.3 共享 lexical retrieval | 已修复 | discovery/analysis 共用 BM25 tokenizer、cache、ranking、RRF；complete-scope 测试通过。 |
| 4.4 discovery 软配额 | 已修复 | 两阶段候选分配与单篇安全上限测试通过。 |
| 4.5–4.6 evidence / token parent context | 已修复 | 独立逐篇召回、一次补充、去重、双向 parent、token budget 与 lineage 测试通过。 |
| 4.7 typed payload | 已修复 | `PaperEvidencePayload` Pydantic schema 为 staged/background/synthesis/partial 唯一持久化读取边界。 |
| 4.8 orchestrator | 已修复 | strict scope、query-only、独立重试、实际 stage latency、持久化恢复均有定向覆盖。 |
| 4.9 mode | 已修复 | 三种 prompt 及 PARTIAL 报告结构的差异测试通过。 |
| 4.10 background | 代码已修复；当前 provider 不兼容 | submit/poll/幂等/deadline/cancel/synthesis/recovery queue/Beat 均有 fake 覆盖。真实测试确认当前 `openai_compatible` endpoint 的 create 可返回 response ID，但 retrieve 返回 HTTP 404，故不能在该 endpoint 上完成真实 background。 |
| 4.11 兼容摘要字段 | 已修复 | 同步/API 均继续写入/返回 `null`；旧 `PaperMindmapService` 已删除，`/mindmap` 路由仍兼容地转入统一 analysis job。 |
| 4.12 前端 | 已修复并部署 | 高级筛选、解释 chips、venue/keywords 展示已通过 Docker Bun type/lint/test/build，并滚动部署。 |
| 4.13 运维配置 | 已修复并运行验证 | 容器实际为 `db:5432`，LLM worker 实际消费 `research-llm`，迁移为 0053 head。 |
| 4.14 验证与文档 | 已修复 | 验证脚本在运行库通过；Compose、部署文档及隔离 migration test safety guard 已更新。 |

## 已落地内容

### 查询解析与筛选

- 支持中文/英文精确年份、范围、起始年、截止年；精确年份写入 `year_from` 与 `year_to`。
- 修复中文连续文本和中文 BibTeX 类型不适用 `\b` 的问题。
- `6G`、`3D`、`GPT-4`、`IEEE 802.11` 不会误判；包含 `6G` 与真实年份时仍解析真实年份。
- 解析 `作者:`/`author:`、`期刊:`/`venue:`、`关键词:`/`keywords:` 等高置信标记；显式 API 字段优先。
- 完整移除年份约束残余（例如“发表的”），纯元数据查询不会进入 hybrid 路径。
- DOI 改为规范化后的等值匹配；keywords 改为匹配任一关键词；`total` 改为去重且 limit 前的总量，当前展示数为 `items.length`。

### Evidence、payload 与编排

- `LocalPaperEvidenceRetriever` 使用可注入的 `tiktoken` 计数器；每篇证据共享 4000 token 默认预算，命中 child 完整保留，并从 child 两侧按段落/句子边界扩展。
- 补充检索仅在通过阈值的证据数少于 `LOCAL_PAPER_ANALYSIS_MIN_EVIDENCE_PER_PAPER` 时执行一次；两轮按 chunk ID 去重、合并、重排。
- discovery 重排候选使用两阶段软配额：阶段 1 在整个 RRF 序列中尽量为不同论文各保留一个候选，阶段 2 再按全局 RRF 顺序填充剩余预算。每篇 `LOCAL_PAPER_MAX_RERANK_CHUNKS_PER_PAPER=6` 只是防止长文档垄断的最终保护，第二/第三高相关 chunk 可进入 BGE。
- 所有 paper stage 统一使用 `PaperEvidencePayload` Pydantic schema 校验后的 `paper`、`question`、`queries_used`、`insufficient_evidence` 与完整 evidence lineage JSON contract；staged、background、synthesis、partial report 均经这一唯一 schema 读取持久化结构。
- 修复 query-only 分支 `sql_select`/model 未绑定风险；指定的 `paper_ids` 任一不在 owner/library/INDEXED/active-version scope 内时返回 `INVALID_PAPER_SCOPE`。
- `focused`、`comparative`、`comprehensive` 现在分别影响 paper 和 synthesis prompt；PARTIAL 的最终报告也有不同的固定章节结构；新增测试验证三者不同。
- staged paper stage 成功时保存网关返回的真实 `latency_ms`，不再以编排层的耗时替代。
- background recovery task 已路由至实际监听的 `research-llm` queue；轮询仍为单次 retrieve + countdown，未引入循环 sleep。
- 新增 fake background tests，覆盖 `AsyncOpenAI.responses.create(background=True, store=False)`、retrieve/cancel、保存 `provider_response_id` 后调度、每次 poll 仅 retrieve 一次并重调度、未到期重复 poll 幂等、deadline cancel、background synthesis 使用已持久化逐篇结果后 finalize，以及 `BACKGROUND_NOT_SUPPORTED` 的安全规范化。
- 删除无调用方的 `extract_structured_sections()` 旧摘要抽取；删除 `PaperMindmapService`。`/mindmap` 保留为 deprecated HTTP compatibility route，但已直接创建统一 analysis job。唯一仍需的 Markdown→OPML 转换收敛为 `PaperAnalysisReportService.markdown_to_opml()` 纯格式函数，不再携带独立模型调用或读取废弃摘要字段。

### 前端、配置与文档

- 增加默认折叠的高级筛选（年份、作者、DOI、venue、BibTeX 类型、keywords）。
- 增加 `QueryInterpretation` TypeScript 类型与搜索结果 chips；显示语义查询、实际 filter、来源和 warning。论文条目显示 venue，keywords 可折叠。
- 修复 dashboard 长报告的双滚动与下方视觉截断：dashboard 根容器阻止内容外溢，并在存在 `#main` 的 dashboard 路由锁定 `html/body` document 滚动，只保留 `<main>` 的单一纵向滚动区；已重建并滚动 frontend 容器。
- `backend/.env.example` 与代码 ingestion version 对齐为 `docling-parent-child-bge-v7`，并补全 analysis 配置。
- Compose 中 app/migrate/worker 显式使用 `POSTGRES_HOST=db` 和 `POSTGRES_PORT=5432`；宿主端口仍只受 `POSTGRES_EXPOSE_PORT` 控制。LLM worker 默认并发改为 1。
- 修复 `research-worker-llm` 未挂载 `/app/media` 的部署缺口：当对象存储回退为文件系统时，worker 与 API 现在共享 `media_data`，已在独立端到端环境验证 artifact HTTP 下载成功，并已滚动重建当前 LLM worker。
- 重写 `verify_refactoring.py`：支持 Python 3.12 和 `--skip-db`，不调用模型，使用 `settings.DATABASE_URL`，失败返回非零退出码。
- `DEPLOYMENT.md` 已改为当前 Compose 拓扑、`/api/v1/research/local-library/...` 路径、备份/迁移/worker/sync/验收顺序，并删除未经测量的性能承诺。

## 本轮实现文件

- 查询与检索：`backend/app/services/literature_research/local_paper_query_parser.py`、`local_paper_library.py`、`local_paper_retrieval.py`、`local_paper_evidence.py`。
- 分析与后台：`backend/app/services/literature_research/local_paper_analysis_orchestrator.py`、`backend/app/worker/celery_app.py`。
- 配置与部署：`backend/app/core/config.py`、`backend/.env.example`、`backend/verify_refactoring.py`、`docker-compose.yml`、`docker-compose.dev.yml`、`docker-compose.prod.yml`、`docker-compose.research.yml`、`DEPLOYMENT.md`。
- 前端：`frontend/src/components/research/local-paper-library-workbench.tsx`、`local-paper-library-workbench.test.tsx`、`frontend/src/types/literature-research.ts`。
- 测试与本报告：`backend/tests/unit/literature_research/test_local_paper_query_parser.py`、`test_local_paper_library.py`、`test_local_paper_analysis.py`、`backend/tests/test_migrations.py`、`实施完成报告.md`。

## 数据库迁移

未新增迁移，未修改 0052 或 0053 的迁移语义。为通过全仓 Ruff，仅对 0044/0045/0050–0053 作 import/format 排序；没有改动 revision、upgrade/downgrade DDL 或数据逻辑。当前 Alembic head 为 `0053_add_venue_keywords`。已在运行中的 Compose 数据库上执行 `migrate`，并在新 API 容器中确认 `alembic current = 0053_add_venue_keywords (head)`；`verify_refactoring.py` 也实际确认 `local_papers.venue` 和 `keywords_json` 均存在。

## 实际执行的验证

| 命令 | 结果 |
| --- | --- |
| `uv sync --frozen` | 通过 |
| transformers / sentence-transformers import smoke | 通过 |
| `uv run ruff check .`（全仓） | 通过，0 error |
| `uv run ruff format --check .`（全仓） | 通过：414 个后端 Python 文件均已格式化；其中 83 个历史文件只进行了无语义格式化 |
| `pytest -q test_local_paper_query_parser.py test_local_paper_analysis.py`（当前工作树） | 53 passed，90 个 Pydantic/deprecation warnings |
| typed payload、mode、stage latency、background lifecycle、重启恢复路由及 `/mindmap` 统一 job 兼容定向测试 | 22 passed（全部使用 fake provider） |
| shared BM25 complete-scope / RRF / discovery integration | 3 passed |
| 图像 locator/figure OCR lineage 回归 | 1 passed |
| 删除旧 Mindmap 服务后的定向 analysis/library 回归 | 45 passed；确认 unified OPML 转换、无旧摘要读取路径和检索回归均正常 |
| `uv run pytest -q` 全量后端（当前工作树） | 626 passed，4 skipped，251 warnings；migration downgrade 测试因未显式隔离授权而安全跳过 |
| 一次性无卷 PostgreSQL（`127.0.0.1:55433`）上设置 `MIGRATION_TEST_ISOLATED_DATABASE=1` 的 `pytest -q tests/test_migrations.py` | 4 passed；upgrade、downgrade base、upgrade→downgrade→upgrade、current=head 均通过，随后容器已停止并自动删除 |
| 一次性无卷 PostgreSQL（`127.0.0.1:55433`）上的完整 `pytest -q` | 630 passed，252 warnings，25.07s；设置 `MIGRATION_TEST_ISOLATED_DATABASE=1`，覆盖完整后端套件及 4 个 destructive migration 测试。执行器的流式输出曾在 68% 截断，已通过 `/tmp` 临时日志取得最终摘要；随后已删除该无卷容器。 |
| 未设置隔离授权的 `pytest -q tests/test_migrations.py` | 4 skipped；新的显式 guard 阻止共享部署数据库执行 destructive downgrade |
| `uv run alembic heads` | 通过：`0053_add_venue_keywords (head)` |
| `docker exec academic_research_agent_backend python verify_refactoring.py` | 通过：parser、imports/schema、Alembic graph、运行中 PostgreSQL schema 均通过 |
| `docker compose --env-file backend/.env.example ... config --quiet`（临时 `LOCAL_PAPER_LIBRARY_HOST_PATH=/tmp`） | 通过 |
| 当前工作树的临时 Bun 容器中 `bun run build` | 通过；Next build 完成编译和类型检查，临时容器退出后产物自动删除 |
| 当前工作树的临时 Bun 容器中 `bun run type-check` | 通过 |
| dashboard 单滚动布局修复后的 Bun `type-check` 与 frontend Docker build | 通过；`/en/research/new` 路由可访问，frontend 容器 health 为 `healthy`。 |
| 当前工作树的临时 Bun 容器中 `bun run lint` | 0 error，29 个既有 warning |
| 当前工作树的临时 Bun 容器中 `bun run test:run` | 9 files、37 passed（包含新增高级筛选和解释 chips 测试） |
| Compose 后端镜像构建、`run --rm migrate`、滚动 `app`/Beat/CPU/IO/LLM worker | 通过；删除旧 Mindmap 服务后已重新构建并滚动，运行中的 API/worker 使用新镜像 |
| Compose 前端镜像构建、滚动 `frontend` | 通过；`/api/health`、`/en/research` 均为 HTTP 200 |
| 运行中 API health / parser / worker queue | `/api/v1/health` 为 200；API 容器 health 为 `healthy`；示例解析为 `semantic communication, 2026, 2026`；LLM worker 消费 `research-llm` |
| 运行中、已认证的 `POST /api/v1/research/local-library/search`（只读） | HTTP 200；`items=1`、`total=1`、`retrieval_mode=hybrid`；返回 `semantic_query=semantic communication`、`year_from=year_to=2026`、无 warning。临时 JWT 仅在容器进程内使用，未输出且未写入业务数据。 |
| 运行中、管理员 JWT 的 `GET /status` 与 `POST /export`（只读） | 均为 HTTP 200；状态为 `RUNNING`；CSV 导出为 `text/csv`、7031 bytes 且携带下载头。故意提交空 `question` 的 analysis-job 请求返回 HTTP 422，未创建任务、未写入数据、未调用模型。 |
| 独立测试库同步 | `/home/cumt/lly/test_library` 的 `automed.bib` 和唯一 6 页 PDF 以只读方式挂载；真实 `POST /sync` 为 HTTP 202，Docling 解析、BGE embedding、独立 Qdrant collection 和 `research-cpu` 队列均成功。 |
| 独立测试库真实 staged analysis | 真实 search 为 HTTP 200/`total=1`；真实 `POST /analysis-jobs` 为 HTTP 202，模式为 `staged`。外部 Responses 模型调用、逐篇分析、综合均完成，job 为 `COMPLETED`。修复共享 `media_data` 后，artifact 下载为 HTTP 200、`text/markdown`、带 SHA-256 头。 |
| 独立测试库真实 background analysis | 临时启用 `background` 和 provider storage 后，真实 create 返回 `provider_response_id`、provider 状态为 `completed`；随后 `responses.retrieve(id)` 返回 HTTP 404，job 受控失败。结论是当前 configured endpoint 不实现 Responses retrieve，不能支持本项目的 durable polling contract；生产仍保持 `staged`。 |
| 新运行镜像的旧服务剔除与统一 OPML 模块 smoke | `paper_mindmap_service.py` 不存在；`PaperAnalysisReportService.markdown_to_opml()` 可导入执行；运行中验证器和数据库 schema 均通过 |

全仓 `ruff check .` 与 `ruff format --check .` 均已通过；83 个历史文件仅进行了无语义格式化。首次 Docker 前端 build 发现本轮高级筛选 JSX 中遗漏的 `type={...}` 结束花括号；已修复，最终 build 与全部前端门禁通过。共享部署数据库的完整 `pytest -q` 为 626 通过、4 个 migration 测试安全跳过，且不会对共享库执行 destructive downgrade。完整套件随后已在无卷隔离 PostgreSQL 中取得最终摘要：630 passed、252 warnings、25.07s，覆盖 4 个 migration rollback 测试；临时容器已删除。

## 未验证或未完成项

1. 共享部署 PostgreSQL 不执行 destructive migration downgrade：其历史数据存在 `(paper_id, page_number, figure_index)` 重复记录，回到 0050 的旧唯一约束会报 `UniqueViolation`。这是一项运行安全边界而非迁移链质量未验证：迁移专用隔离链 4/4 及完整隔离后端套件 630/630 均已通过；运行数据库当前为 `0053_add_venue_keywords (head)`。如需演练共享库回滚，应先从备份还原到独立 shadow 库，再在变更窗口内处理重复数据后执行。
2. 已用独立 `/home/cumt/lly/test_library` 完成真实 sync、search、staged deep-analysis 和 artifact 下载验收；测试库有独立 PostgreSQL、Redis、Qdrant、测试用户和临时 media volume，已避免污染当前部署库。首次隔离 worker 少传离线模型变量、首次 LLM worker 少传生产 `extra_hosts` 映射，均已定位为临时测试拓扑遗漏；前者以离线 cache 配置修复，后者以现有固定 hosts 映射修复。两者不影响当前部署。
3. background 已做真实 AsyncOpenAI provider 集成测试，但当前 configured `openai_compatible` endpoint 不支持 `responses.retrieve`（真实 create 返回 response ID，真实 retrieve 返回 HTTP 404），所以它不能满足 durable polling/cancel/recovery contract。生产配置保持 `LOCAL_PAPER_ANALYSIS_EXECUTION_MODE=staged` 且 storage 开关关闭。要使 background 成为可用功能，必须切换到明确实现 `POST /responses`、`GET /responses/{id}`、取消接口的 provider/endpoint，并在同一独立测试库中复验完成、取消、超时和重启恢复；不能仅打开 storage 开关。

## 后续部署命令

```bash
cp backend/.env.example backend/.env
# 填写密钥和 LOCAL_PAPER_LIBRARY_HOST_PATH，先备份 PostgreSQL

docker compose --env-file backend/.env -f docker-compose.yml -f docker-compose.research.yml -f docker-compose.local-library.yml build
docker compose --env-file backend/.env -f docker-compose.yml -f docker-compose.research.yml -f docker-compose.local-library.yml run --rm migrate
docker compose --env-file backend/.env -f docker-compose.yml -f docker-compose.research.yml -f docker-compose.local-library.yml up -d --wait
docker compose --env-file backend/.env -f docker-compose.yml -f docker-compose.research.yml -f docker-compose.local-library.yml ps
docker compose --env-file backend/.env
