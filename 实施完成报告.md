# 本地论文库重构 - 实施完成报告

## 执行状态：70% 完成，可以部署测试

**日期**：2026-08-27  
**项目**：academic_research_agent  
**原始需求**：ZOTERO_LOCAL_LIBRARY_FIX_PLAN.md

---

## 核心改进

### 问题
1. 查询 "2026年发表的 semantic communication" 按字面搜索，返回所有年份的论文
2. 分析时混合了检索查询和分析问题，导致无关证据
3. 证据使用截断的父文本前缀和废弃的 abstract/intro/conclusion 字段

### 解决方案
1. **查询解析器**：自动从自然语言提取年份/DOI/类型过滤器
2. **搜索集成**：使用清理后的语义查询进行向量/BM25检索
3. **分析证据**：每篇论文独立检索，使用分析问题（不混合检索查询）
4. **证据结构**：使用完整的父子结构，包含section类型和完整血统

---

## 已完成的实现

### 1. 查询解析器 ✅
**文件**：`backend/app/services/literature_research/local_paper_query_parser.py`（400行新代码）

提取：
- 年份过滤（精确、范围、从、到）
- DOI模式
- BibTeX类型别名（期刊论文 → article）
- 避免误识别（6G、3D、GPT-4、IEEE 802.11）

**测试**：30+ 测试用例，全部通过

### 2. 数据库架构 ✅
**文件**：
- `backend/app/db/models/local_paper_library.py`（+20行）
- `backend/app/schemas/literature_research/local_library.py`（+60行）
- `backend/alembic/versions/0053_venue_keywords_deprecate_sections.py`（新增）

**新增字段**：
- `LocalPaper.venue`（规范化的期刊/会议名）
- `LocalPaper.keywords_json`（关键词列表）
- 证据血统：`chunk_id`, `section_id`, `document_version_id`, `section_type`
- 搜索响应中的 `QueryInterpretation`

**废弃字段**（向后兼容）：
- `abstract_text`, `introduction_text`, `conclusion_text` → 始终返回 `None`
- 数据库列保留以支持回滚

### 3. 同步更新 ✅
**文件**：`backend/app/services/literature_research/local_paper_bibtex_catalog.py`（+50行）

新增函数：
```python
def publication_year(year, date) -> int | None  # 年份缺失时回退到date字段
def venue(entry) -> str | None  # 从journal/booktitle提取
def keywords(entry) -> list[str]  # 解析Better BibTeX关键词
```

同步行为：
- 填充 `venue` 和 `keywords_json`
- 废弃字段设为 `None`
- 新增和更新路径都使用年份回退

### 4. 搜索集成 ✅
**文件**：`backend/app/services/literature_research/local_paper_library.py`（+150行）

查询处理流程：
1. 解析查询 → 提取过滤器
2. 将过滤器应用到PostgreSQL
3. 使用清理后的 `semantic_query` 进行向量/BM25/重排（不使用原始查询）
4. 返回 `query_interpretation` 显示解析结果

证据：
- 包含完整血统字段
- 父内容可用（不截断）
- `_paper_read()` 对废弃字段返回 `None`

### 5. 检索核心 ✅
**文件**：`backend/app/services/literature_research/local_paper_retrieval.py`（250行新代码）

可复用组件：
```python
class LocalPaperChunkRetriever:
    async def retrieve(...) -> list[RetrievedChunk]:
        """Dense + BM25 + RRF + substantive filter + rerank"""
```

返回带完整血统的 `RetrievedChunk`。不包含MMR或论文选择（由调用者负责）。

### 6. 分析证据检索器 ✅
**文件**：`backend/app/services/literature_research/local_paper_evidence.py`（280行新代码）

每篇论文独立检索：
```python
class LocalPaperEvidenceRetriever:
    async def retrieve_for_papers(
        paper_ids,
        question,  # 不混合检索查询
        query_context=None,
        max_evidence_per_paper=6,
        target_tokens_per_paper=4000,
    ) -> list[PaperEvidenceResult]
```

特性：
- 独立的每篇论文检索（无跨论文MMR）
- Token预算分配（约每篇4000 tokens）
- 上下文构建：始终包含完整子块，双向扩展父文本
- 不足时有界的补充检索

### 7. 分析编排器 ✅
**文件**：`backend/app/services/literature_research/local_paper_analysis_orchestrator.py`（+100行）

证据准备：
- 如果提供 `paper_ids`：严格按 owner/library/INDEXED/active_version 加载
- 否则：先进行发现搜索
- 使用 `question` 调用 `LocalPaperEvidenceRetriever`
- 在stage evidence中存储完整血统

提示词：
- 使用基于section的证据，带句柄 `[E1]`, `[E2]`
- 子块周围的上下文（不截断父前缀）
- 不引用废弃字段

---

## 未实现的部分

### 发现软配额策略 ❌
**当前**：重排前每篇论文硬限制2个块  
**计划**：软配额（先给每篇论文1个块，然后填充预算）  
**影响**：中等（多样性可以更好）

### 模式特定提示 ❌
**当前**：所有模式使用相同提示模板  
**计划**：`focused`、`comparative`、`comprehensive` 有不同策略  
**影响**：低（模式已存储但未区分）

### 前端高级过滤器 ❌
**当前**：只有单一搜索框  
**计划**：可折叠的高级过滤器UI、query_interpretation芯片  
**影响**：高（面向用户的功能，但后端已支持）

---

## 文件清单

### 新增（6个文件）
1. `backend/app/services/literature_research/local_paper_query_parser.py` (400行)
2. `backend/app/services/literature_research/local_paper_retrieval.py` (250行)
3. `backend/app/services/literature_research/local_paper_evidence.py` (280行)
4. `backend/tests/unit/literature_research/test_local_paper_query_parser.py` (650行)
5. `backend/alembic/versions/0053_venue_keywords_deprecate_sections.py` (80行)
6. `backend/verify_refactoring.py` (200行)

### 修改（5个文件）
1. `backend/app/db/models/local_paper_library.py` (+20行)
2. `backend/app/schemas/literature_research/local_library.py` (+60行)
3. `backend/app/services/literature_research/local_paper_bibtex_catalog.py` (+50行)
4. `backend/app/services/literature_research/local_paper_library.py` (+150行)
5. `backend/app/services/literature_research/local_paper_analysis_orchestrator.py` (+100行)

**总计**：约2,240行新增代码，6个新文件，5个修改文件

---

## 部署步骤

```bash
# 1. 应用数据库迁移
cd backend
uv run alembic upgrade head

# 2. 重启服务
sudo systemctl restart academic_research_agent

# 3. 同步填充新字段
uv run academic_research_agent cmd sync-local-library

# 4. 验证
python verify_refactoring.py
```

## 快速测试

```bash
# 测试查询解析
curl -X POST http://localhost:8000/api/research/local-library/search \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"query": "2026年发表的 semantic communication", "limit": 10}'
```

**期望结果**：
- 只返回2026年的论文
- `query_interpretation.effective_filters` 包含 `year_from=2026, year_to=2026`
- `query_interpretation.semantic_query` 是 "semantic communication"

---

## 验证

✅ 所有Python文件编译成功  
✅ 所有导入正常工作  
✅ 查询解析器30+测试通过  
✅ 向后兼容（废弃字段保留在DB中）  
✅ 迁移是增量的（回滚安全）

---

## 测试清单

### 查询解析器
- [ ] "2026年发表的 semantic communication" → 年份过滤2026，语义查询 "semantic communication"
- [ ] "2024-2026年的论文" → year_from=2024, year_to=2026
- [ ] "6G wireless" → 无年份过滤（避免误识别）

### 搜索
- [ ] 响应包含 `query_interpretation`
- [ ] 只返回过滤后的论文
- [ ] 证据包含血统（chunk_id、section_id等）
- [ ] 废弃字段为 `null`

### 分析
- [ ] 选定的论文全部被分析（无MMR损失）
- [ ] 证据使用 `question` 进行检索
- [ ] 提示使用 `[E1]`, `[E2]` 句柄
- [ ] 上下文包含完整子块
- [ ] 不引用废弃字段

---

## 回滚计划

```bash
# 1. 回滚迁移
uv run alembic downgrade -1

# 2. 恢复代码
git revert <commit-hash>

# 3. 重启服务
sudo systemctl restart academic_research_agent

# 4. 如需要从备份恢复
psql your_database < backup.sql
```

---

## 下一步

### 立即（生产前）
1. 部署到测试环境
2. 运行验证脚本
3. 手动端到端测试
4. 监控日志错误

### 短期（第1周）
1. 实现发现软配额
2. 编写集成测试
3. 修复测试中发现的bug

### 中期（第2-3周）
1. 实现前端高级过滤器UI
2. 实现模式特定提示
3. 性能优化

---

## 成功指标

部署后验证：
- 查询解析准确率 >95%
- 所有选定论文被分析（无损失）
- 搜索<2秒，5篇论文分析<30秒
- 错误率<1%

---

**准备者**：AI Agent  
**状态**：可以测试  
**完成度**：70%
